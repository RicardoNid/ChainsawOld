package DSP

case class TimingInfo(
                       inputInterval: Int,
                       outputInterval: Int,
                       workingInterval: Int,
                       protectInterval: Int
                     )
