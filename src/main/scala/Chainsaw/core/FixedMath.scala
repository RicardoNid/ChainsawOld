package Chainsaw.core



/** math strategy for fixed-point arithmetic, the default behavior is the same as verilog default(full precision)
 *
 * we use the strategies which has the same name as they are in the matlab fixed point designer
 */
case class FixedMath()
